# HEIC test fixture

`libheif-example.heic` is the example image from the upstream
[`strukturag/libheif`](https://github.com/strukturag/libheif/blob/master/examples/example.heic)
repository. It is retained only to validate local browser decoding. The upstream
project's copyright and LGPL-3.0 license apply.
